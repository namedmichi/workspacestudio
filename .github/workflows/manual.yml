# This is a basic workflow that is manually triggered

name: RefrehStats

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'Person to greet'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: false
        # The data type of the input
        type: string

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  greet:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - uses: actions/checkout@v1
      - uses: JamesIves/github-pages-deploy-action@v4.4.0
        with:
             token: ${{ secrets.GITHUB_TOKEN }}
      - name: Run a one-line script
        run: bash getCounter.sh
      - name: setup Github1
        run: |
            git config --global credential.helper 'cache --timeout 3600'
            hub config --global hub.protocol https
            hub config --global user.email "j.technology@web.de"
            hub config --global user.name "namedmichi"
            export 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=$token
            hub remote set-url origin https://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:x-oauth-basic@github.com/namedmichi/workspacestudio
            git status
            git add .
            git commit -m "update Stats"
            git fetch origin master
            hub push origin HEAD:master
